# ========================================
# Dashboard (Frontend) Environment Variables
# ========================================

# The HTTP API URL pointing to the ai-agent backend
NEXT_PUBLIC_API_URL="http://localhost:3000/api"

# The WebSocket URL pointing to the ai-agent backend
NEXT_PUBLIC_WS_URL="ws://localhost:3000"

# The port the Next.js dashboard runs on
PORT=3001
